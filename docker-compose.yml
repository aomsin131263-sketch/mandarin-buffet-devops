version: '3.8'

services:
  # Service หน้าร้าน Mandarin Buffet
  mandarin-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: mandarin_buffet_app
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production